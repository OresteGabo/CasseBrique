/*#include"doctest.h"
#include"Cercle.h"


TEST_CASE("Tests pour un cercle") {
    SUBCASE("La construction d'un cercle"){
        Cercle c{10.1,Position(10,10)};
        REQUIRE_EQ(c.position(),Position(10,10));
        REQUIRE_EQ(c.diametre(), 10.1*2);
        REQUIRE_EQ(c.diametre(), 10.1*2);
        REQUIRE_EQ(c.diametre(), 10.1*2);
    }
}
TESTCASE("Les tests des fonction qui nous renvois les valeurs"){
    Cercle c(10.1,Position(10,10));
    SUBCASE(" diametre"){
        REQUIRE_EQ(c.diametre(),10.1*2);
    }
    SUBCASE(" rayon"){
        REQUIRE_EQ(c.rayon(),10.1)
    }
    SUBCASE(" position"){
        REQUIRE_EQ(c.position(),Position(10,10));
    }
}
*/
